# QUARANTINED — DO NOT LOAD

`xgb_model.RANDOM-DATA-DO-NOT-USE.pkl` was trained on `numpy.random` dummy data.
Its predictions are meaningless noise. It lives here — outside every model load
path — so no code can pick it up by accident. See `../README.md`.
