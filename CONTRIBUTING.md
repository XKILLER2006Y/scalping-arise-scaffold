# Contributing
- Follow README as source of truth.
- Branch: `feat/phase4-extension`, `fix/...`
- Run `pytest -q` before PR. No new phase logic without schema lock.
- Never commit `.env`, keys, venv, node_modules, .next.
